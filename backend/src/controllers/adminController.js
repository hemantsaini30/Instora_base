// Placeholder — Phase 2 will add full admin features
const getDashboard = async (req, res) => {
  res.json({ success: true, message: 'Admin dashboard data — coming in Phase 2' });
};

module.exports = { getDashboard };